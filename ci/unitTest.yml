---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tags: 3.5.4-jdk-8-alpine

inputs:
  - name: chaos-engine

caches:
  - path: maven

run:
  path: chaos-engine/ci/unitTest.sh