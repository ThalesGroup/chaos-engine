platform: linux

image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
- name: git-chaos-engine

outputs:
- name: version

params:
  BRANCH:

run:
  path: ./git-chaos-engine/ci/build/tag-build.sh
  args: []